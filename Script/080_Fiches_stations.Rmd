---
title: '**Fiche station `r params$code_station`**'
author: "`r params$auteur`"
date: "`r format(Sys.time(), '%d %B %Y')`"
output:
  word_document:
    toc: false
  pdf_document:
    latex_engine: xelatex
    toc: false
  html_document:
    highlight: pygments
    theme: default
    toc: false
    css: css/ofb_theme_html.css
params:
  code_station: 03176945
  auteur: "OFB DR Normandie"
  annee_debut: 2007
  annee_fin: 2024
editor_options:
  chunk_output_type: console
knit: (function(input_file, encoding) {

  code_station <- rmarkdown::yaml_front_matter(input_file)$params$code_station

  rmarkdown::render(
    input_file,
    encoding = encoding,
    output_format = "pdf_document",
    output_file = file.path ( "../Output/8_Fiches_stations",paste0("Fiche_station_", code_station, ".pdf"))
  )})
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, 
                      warning = FALSE,
                      message = FALSE)
```

```{r, echo = FALSE}
htmltools::img(src = knitr::image_uri(here::here("Script/css", "logo_OFB_v2.png")),
               alt = 'logo',
               style = 'position:absolute; top:0; right:0; padding:10px; width:180px;')
```



> ⚠ **Préambule** :<br>
> 
> Cette fiche opérationnelle présente l'évolution des différentes indices d'état biologique des cours d'eau étudiés dans le cadre de la Directive Cadre sur l'Eau. <br>
> Sur chaque station suivie en Normandie dans le cadre de la pêche scientifique à l'électricité, 3 autres communautés biologiques sont aussi échantillonnées; macrophytes, macroinvertébrés aquatiques et diatomées. <br>
> Les prélèvements éffectués permettent ensuite de calculer les indices d'état biologique des cours d'eau sivants: Indice Poisson Rivière (IPR), Indice Biologique Macrophyte en Rivière (IBMR), Indice macro-invertébré Multimétrique (I2M2) et Indice Biologique Diatomée (IBD).<br>
> Ces indices donnent des résultats sous forme de classe d'état biologique, allant de "Très Bon" à "Mauvais". <br>

```{r message=FALSE, warning=FALSE, echo = FALSE}
library(tidyverse)
library(sf)
library(ggtext)
library(gt)
library(ggh4x)
library(colorspace)
library(here)
library(glue)
library(ggrepel)
library(tinytex)
```

```{r message=FALSE, warning=FALSE, echo = FALSE}
info_stations <- readRDS(here("Data/clean_data", "BDD_info_stations.rds"))
stations_envt <- readRDS(here("Data/clean_data", "context_stations_complet.rds"))
element_declassant <- readRDS(here("Data/clean_data", "table_element_declassant.rds"))

valeurs_seuils <- readRDS(here("Data/clean_data", "valeurs_seuils_indices.rds"))
load(here("Data/clean_data", "results_LMM.rda"))
BDD_indices <- readRDS(here("Data/clean_data", "BDD_indices.rds"))

BDD_metriques <- readRDS(here("Data/clean_data", "BDD_metriques.rds"))

mes_stations <- readRDS(here("Data/clean_data", "liste_stations_retenues.rds"))
```


```{r message=FALSE, warning=FALSE, echo = FALSE}
source(here::here("R", "fun_classe_ecologique.R"))
source(here::here("R/fiches_stations", "fun_table_info_station.R"))
source(here::here("R/fiches_stations", "fun_graph_landuse.R"))
source(here::here("R/fiches_stations", "fun_table_element_declassant.R"))
source(here::here("R/fiches_stations", "fun_graph_lmm.R"))
source(here::here("R/fiches_stations", "fun_graph_metriques.R"))
source(here::here("R/fiches_stations", "fun_carte_localisation.R"))
```


# 1. Informations sur la station
<br>


```{r}
a<-carte_station(id_station=params$code_station)

b <-create_table(data=info_stations,
    id_station = params$code_station)
```

::: {.columns}
::: {.column width="60%"}
```{r}
a
```
:::

::: {.column width="40%"}
```{r}
b
```
:::
:::
<br>
<br>
<br>

# 2. Occupation du sol
<br>

```{r, fig.width=6.5, fig.height=4, out.width="60%", fig.align='center'}
landuse(data=stations_envt,
        id_station=params$code_station)
```
<br>
<br>
<br>

# 3. Etat biologique de la station au cours du temps
<br>

```{r message=FALSE, out.width="70%"}
tab_element_declassant(
    data = element_declassant,
    id_station = params$code_station
)
```
<br>
<br>
<br>

# 4. Tendance temporelle des indices d'état biologique
<br>

```{r, fig.width=12, fig.height=9, out.width="100%", fig.align='center'}
graph_lmm(data=BDD_indices,
          tendances_RCS = valeurs_lmm_RCS,
          tendances_RRP = valeurs_lmm_RRP,
          tendances_stations = valeurs_lmm_station,
          id_station= params$code_station)
```
<br>
<br>
<br>

# 5. Evolution des métriques de diversité
<br>
4 métriques de diversité ont été calculées pour chaque compartiment biologique:<br>
- **Abondance totale** = nombre total d'individus échantillonnés;<br>
- **Richesse spécifique** = nombre total d'espèces;<br>
- **Indice de diversité de Shannon** = mesure de la diversité des espèces au sein d’une communauté en prenant en compte la proportion de chaque espèce dans la communauté. Lorsque cet indice est égal à 0, cela signifie que tous les individus appartiennent à la même espèce.<br>
- **Indice d’équitabilité de Piélou** = mesure de la répartition des espèces au sein d’une communauté donnée. lorsque cet indice est proche 0 ça traduit une dominance de la communauté par une espece, lorsqu'il est proche de 1 ça traduit une distribution des individus uniforme entre toutes les espèces.<br>
<br>


```{r, fig.width=13, fig.height=11, out.width="100%", fig.align='center'}
graph_metriques(data=BDD_metriques,
                id_station=params$code_station)
```
<br>
<br>
<br>

