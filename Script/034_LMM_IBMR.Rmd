---
title: "023_LMM_IPR"
author: "Julia"
date: "2026-02-23"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

 Script permettant de visualiser l'évolution des indices au cours des années à l'échelle de la Normandie

# Installation
## Chargement des packages
```{r message=FALSE, warning=FALSE}
library(tidyverse)
library(lubridate)
library(here)
library(lme4)
library(ggtext)
library(DHARMa)
library(MuMIn)
library(lmerTest)
library(car)
```

## Chargement des fonctions
```{r}
source(here::here("R", "fun_classe_ecologique.R"))
```

## Chargement des données
```{r}
BDD_indices <- readRDS(here("Data/clean_data", "BDD_indices.rds"))
mes_stations <- readRDS(here("Data/clean_data", "liste_stations_retenues.rds"))
valeurs_seuils <- readRDS(here("Data/clean_data", "valeurs_seuils_indices.rds"))
```

```{r}
BDD_IBMR <- BDD_indices %>% 
  filter(code_station %in% mes_stations,
         libelle_indice=="IBMR")
```


# Réseau RRP

## Préparer les données
```{r}
BDD_IBMR_RRP <- BDD_IBMR%>% 
  filter(reseaux=="RRP",
         annee >=2013)%>% 
  group_by(code_station) %>%
  mutate(annee_c = (annee - min(annee) + 1),
         annee_min = min(annee),
         julian=lubridate::yday(date_prelevement),
         resultat_log=log(resultat_eqr+1)) 
```

## Plot pour vérifier visuellement la normalité
```{r}
ggplot(BDD_IBMR_RRP, aes(x=resultat_log))+
  geom_histogram(show.legend = FALSE, color = "black")+
  theme_bw()+
  labs(title="données brutes",
       x="Résultat",
       y="Nombre")

#Observer distribution des jours julian
ggplot(BDD_IBMR_RRP, aes(x=julian))+
  geom_histogram(show.legend = FALSE, color = "black")+
  theme_bw()+
  labs(title="distribution jour julian",
       x="jour julian",
       y="Nombre")
```

## LMM sur les données IBMR 

Hypothèse à tester: l'évolution de la valeur de l'indice dépend des stations 

### Ecriture des modeles possibles
```{r}
M_brute_RRP <- lmer(resultat_eqr~ annee_c + (1 + annee_c | code_station)+ julian +operateur +libelle_typologie + annee_min, data = BDD_IBMR_RRP, REML=FALSE, lmerControl(optimizer = "bobyqa")) 

sim_brute_RRP <- simulateResiduals(M_brute_RRP)
plot(sim_brute_RRP, main = "DHARMa - M_brute_RRP")
```

###Tester la multicolinéarité
```{r}
vif(M_brute_RRP)
```
vif environ = à 1 donc pas de multicolinéarité, modele fiable


## Tester toutes les combinaisons de modèles et déterminer les variables ayant un effet significatif
```{r}
options(na.action = "na.fail")
dredge_brute_RRP <- dredge(M_brute_RRP)

summary(model.avg(dredge_brute_RRP, subset = delta < 2))
```
Effet de l'année non significatif
Effet de julian significatif



### Vérifier l'homogénéité des résidus
```{r}
# Plotez les valeurs prédites par rapport aux valeurs résiduelles
par(mar = c(4, 4, 0.5, 0.5))
plot(resid(M_brute_RRP) ~ fitted(M_brute_RRP), xlab = "Predicted values", ylab = "Normalized residuals")
abline(h = 0, lty = 2)
```
La variance est homogène


### Vérifier l'indépendance des résidus
```{r}
par(mfrow = c(1, 3), mar = c(4, 4, 0.5, 0.5))

plot(resid(M_brute_RRP) ~ BDD_IBMR_RRP$resultat_eqr, xlab = "I2M2", ylab = "Normalized residuals")
abline(h = 0, lty = 2)

boxplot(resid(M_brute_RRP) ~ annee_c, data = BDD_IBMR_RRP, xlab = "Années",
    ylab = "Normalized residuals")
abline(h = 0, lty = 2)

boxplot(resid(M_brute_RRP) ~ code_station, data = BDD_IBMR_RRP, xlab = "stations", ylab = "Normalized residuals")
abline(h = 0, lty = 2)
```
Il semble que l'indépendance des résidus soit vérifiée

## Interprétation du modèle
```{r}
Modele_IBMR_RRP <- lmer(
  resultat_eqr ~ annee_c + (1 + annee_c |
                              code_station) ,
  data = BDD_IBMR_RRP,
  REML = TRUE,
  control = lmerControl(optimizer = "bobyqa")
)

# Résumé avec p-values
summary(Modele_IBMR_RRP)

# Résumé
coef_table_RRP <- as.data.frame(coef(summary(Modele_IBMR_RRP)))
names(coef_table_RRP) <- c("Estimate", "Std_Error", "df", "t_value", "p_value")

# Extraire les valeurs
intercept_est  <- coef_table_RRP["(Intercept)", "Estimate"]
intercept_p    <- coef_table_RRP["(Intercept)", "p_value"]

annee_est      <- coef_table_RRP["annee_c", "Estimate"]
annee_p        <- coef_table_RRP["annee_c", "p_value"]

# R²
r2_vals <- r.squaredGLMM(Modele_IBMR_RRP)
r2_marg <- r2_vals[1]
r2_cond <- r2_vals[2]

# Construire un tableau déjà tidy
donnees_model_IBMR_RRP <- data.frame(
  libelle_indice = "IBMR",
  reseaux = "RRP",
  intercept_estimate = intercept_est,
  intercept_pvalue = intercept_p,
  annee_estimate = annee_est,
  annee_pvalue = annee_p,
  R2_marginal = r2_marg,
  R2_conditionnel = r2_cond
)

# Arrondir
donnees_model_IBMR_RRP <- donnees_model_IBMR_RRP %>%
  mutate(across(where(is.numeric), ~ round(., 3)))
```



## Retranformer les données pour faire le plot sur les valeurs brutes
```{r}
newdata_RRP <- data.frame(
  annee = seq(min(BDD_IBMR_RRP$annee), max(BDD_IBMR_RRP$annee), length.out = 200)
)
newdata_RRP$annee_c <- newdata_RRP$annee - min(BDD_IBMR_RRP$annee) + 1
pred <- predict(Modele_IBMR_RRP, newdata_RRP, re.form = NA, se.fit = TRUE)

newdata_RRP$fit   <- pred$fit
newdata_RRP$lower <- pred$fit - 1.96 * pred$se.fit
newdata_RRP$upper <- pred$fit + 1.96 * pred$se.fit
```




# Réseau RCS

## Préparer les données
```{r}
BDD_IBMR_RCS <- BDD_IBMR%>% 
  filter(reseaux=="RCS") %>% 
  group_by(code_station) %>%
  mutate(annee_c = (annee - min(annee) + 1),
         annee_min = min(annee),
         julian=lubridate::yday(date_prelevement))
```



## Plot pour vérifier visuelement la normalité
```{r}
ggplot(BDD_IBMR_RCS, aes(x=resultat_eqr))+
  geom_histogram(show.legend = FALSE, color = "black")+
  theme_bw()+
  labs(title="données brutes",
       x="Résultat",
       y="Nombre")

#Observer distribution des jours julian
ggplot(BDD_IBMR_RCS, aes(x=julian))+
  geom_histogram(show.legend = FALSE, color = "black")+
  theme_bw()+
  labs(title="Distribution jour julian",
       x="jour julian",
       y="Nombre")
```

## LMM sur les données brutes

Hypothèse à tester: l'évolution de la valeur de l'indice dépend des stations 

```{r}
M_brute_RCS <- lmer(resultat_eqr ~ annee_c + (1 + annee_c | code_station)+ julian +operateur +libelle_typologie + annee_min, data = BDD_IBMR_RCS, REML=FALSE, lmerControl(optimizer = "bobyqa")) 

sim_brute_RCS <- simulateResiduals(M_brute_RCS)
plot(sim_brute_RCS, main = "DHARMa - M_brute_RCS")
```

###Tester la multicolinéarité
```{r}
vif(M_brute_RCS)
```
vif environ = à 1 donc pas de multicolinéarité, modele fiable


##Tester toutes les combinaisons de modèles et déterminer les variables ayant un effet significatif
```{r}
options(na.action = "na.fail")
dredge_brute_RCS <- dredge(M_brute_RCS)

summary(model.avg(dredge_brute_RCS, subset = delta < 2))
```
Effet significatif de annee_c, julian et typologie

## Identifier et retirer les outliers

Pour les retirer, on a besoin de leur attribuer un identifiant.
```{r}
# Ajouter un identifiant unique
BDD_IBMR_RCS$id_obs <- seq_len(nrow(BDD_IBMR_RCS))

M_brute_RCS <- lmer(resultat_eqr ~ annee_c + (1 + annee_c | code_station) +id_obs, data = BDD_IBMR_RCS, REML=FALSE , lmerControl(optimizer = "bobyqa")) #pente aléatoire et intercept aléatoire en fonction des stations


# Résidus DHARMa
sim_brute_RCS<- simulateResiduals(M_brute_RCS, plot = FALSE)

# Indices des outliers
indices_outliers_RCS <- outliers(sim_brute_RCS, return = "index")

# Données du modèle
df_model_RCS <- model.frame(M_brute_RCS)
outlier_table <- df_model_RCS[indices_outliers_RCS, ]

# on a 5 outliers réparti en 4 stations

# Identifiants des outliers
id_outliers_RCS <- df_model_RCS$id_obs[indices_outliers_RCS]

# Retirer les outliers du jeu de données original
BDD_IBMR_RCS_clean <- BDD_IBMR_RCS %>%
  filter(!id_obs %in% id_outliers_RCS)
```

```{r}
M_brute_RCS_clean <- lmer(resultat_eqr ~ annee_c + (1 + annee_c | code_station), data = BDD_IBMR_RCS_clean, REML=FALSE,lmerControl(optimizer = "bobyqa")) 

sim_brute_RCS_clean <- simulateResiduals(M_brute_RCS_clean)
plot(sim_brute_RCS_clean, main = "DHARMa - M_brute_RCS_clean")
```


### Vérifier l'homogénéité des résidus
```{r}
# Plotez les valeurs prédites par rapport aux valeurs résiduelles
par(mar = c(4, 4, 0.5, 0.5))
plot(resid(M_brute_RCS_clean) ~ fitted(M_brute_RCS_clean), xlab = "Predicted values", ylab = "Normalized residuals")
abline(h = 0, lty = 2)
```
La variance est homogène


### Vérifier l'indépendance des résidus
```{r}
# Afin de vérifier l'indépendance des résidus du modèle,
# nous devons tracer les résidus en fonction de chaque
# covariable du modèle.
par(mfrow = c(1, 3), mar = c(4, 4, 0.5, 0.5))

plot(resid(M_brute_RCS_clean) ~ BDD_IBMR_RCS_clean$resultat_eqr, xlab = "I2M2", ylab = "Normalized residuals")
abline(h = 0, lty = 2)

boxplot(resid(M_brute_RCS_clean) ~ annee_c, data = BDD_IBMR_RCS_clean, xlab = "Années",
    ylab = "Normalized residuals")
abline(h = 0, lty = 2)

boxplot(resid(M_brute_RCS_clean) ~ code_station, data = BDD_IBMR_RCS_clean, xlab = "stations", ylab = "Normalized residuals")
abline(h = 0, lty = 2)
```

## Interprétation du modèle

```{r}
Modele_IBMR_RCS <- lmer(resultat_eqr ~ annee_c + (1 + annee_c | code_station), data = BDD_IBMR_RCS_clean, REML=TRUE,control=lmerControl(optimizer = "bobyqa")) 

# Résumé avec p-values
summary(Modele_IBMR_RCS)

# Résumé
coef_table_RCS <- as.data.frame(coef(summary(Modele_IBMR_RCS)))
names(coef_table_RCS) <- c("Estimate", "Std_Error", "df", "t_value", "p_value")

# Extraire les valeurs
intercept_est  <- coef_table_RCS["(Intercept)", "Estimate"]
intercept_p    <- coef_table_RCS["(Intercept)", "p_value"]

annee_est      <- coef_table_RCS["annee_c", "Estimate"]
annee_p        <- coef_table_RCS["annee_c", "p_value"]

# R²
r2_vals <- r.squaredGLMM(Modele_IBMR_RCS)
r2_marg <- r2_vals[1]
r2_cond <- r2_vals[2]

# Construire un tableau déjà tidy
donnees_model_IBMR_RCS <- data.frame(
  libelle_indice = "IBMR",
  reseaux = "RCS",
  intercept_estimate = intercept_est,
  intercept_pvalue = intercept_p,
  annee_estimate = annee_est,
  annee_pvalue = annee_p,
  R2_marginal = r2_marg,
  R2_conditionnel = r2_cond
)

# Arrondir
donnees_model_IBMR_RCS <- donnees_model_IBMR_RCS %>%
  mutate(across(where(is.numeric), ~ round(., 3)))
```


## Retranformer les données pour faire le plot sur les valeurs brutes
```{r}
newdata_RCS <- data.frame(
  annee = seq(min(BDD_IBMR_RCS_clean$annee), max(BDD_IBMR_RCS_clean$annee), length.out = 200)
)
newdata_RCS$annee_c <- newdata_RCS$annee - min(BDD_IBMR_RCS_clean$annee) + 1
pred <- predict(Modele_IBMR_RCS, newdata_RCS, re.form = NA, se.fit = TRUE)

newdata_RCS$fit   <- pred$fit
newdata_RCS$lower <- pred$fit - 1.96 * pred$se.fit
newdata_RCS$upper <- pred$fit + 1.96 * pred$se.fit
```




# Graphiques 

## Tendances générales des deux réseaux
```{r}
bornes_IBMR <- list(
  #bornes pour l'IBMR
  "2928"=list(
    borne_inf = c(0,0.51,0.64,0.77,0.92),
    borne_sup = c(0.51,0.64,0.77,0.92,1),
    couleurs  = c("red","orange","yellow","lightgreen","#2b83ba"),
    classe_libelle = c("Mauvais", "Médiocre", "Moyen", "Bon", "Très bon")
  )
)

classes_generales_IBMR <- creation_classes(col_code= "2928", 
                                bornes_indices=bornes_IBMR)
```

```{r}
Tendances_generales <- ggplot(BDD_IBMR, aes(x = annee, y = resultat_eqr)) +
  ggplot2::geom_rect(
      data = classes_generales_IBMR,
      ggplot2::aes(
        ymin = classe_borne_inf,
        ymax = classe_borne_sup,
        fill = classe_libelle
      ),
      xmin = -Inf,
      xmax = Inf,
      alpha = 0.2,
      inherit.aes = FALSE
    ) +
  ggplot2::scale_fill_manual(
      name = "Etat biologique",
      values = setNames(classes_generales_IBMR$classe_couleur, classes_generales_IBMR$classe_libelle),
      guide = ggplot2::guide_legend(reverse = TRUE)
    ) +
  geom_point(colour = "black", size = 1
             , alpha = 0.1) +
  geom_line(
    data = newdata_RCS,
    aes(x = annee, y = fit, colour = "Tendance générale RCS"),
    linewidth = 1.3,
    inherit.aes = FALSE
  ) +
  geom_line(
  data = newdata_RCS,
  aes(x = annee, y = lower),
  linetype = "dotted",
  colour = "#CD6839",
  linewidth = 1,
  alpha=1,
  inherit.aes = FALSE
) +
geom_line(
  data = newdata_RCS,
  aes(x = annee, y = upper),
  linetype = "dotted",
  colour = "#CD6839",
  linewidth = 1,
  alpha=1,
  inherit.aes = FALSE
) +
  geom_line(
    data = newdata_RRP,
    aes(x = annee, y = fit, colour = "Tendance générale RRP"),
    linewidth = 1,
    inherit.aes = FALSE
  ) +
  geom_line(
  data = newdata_RRP,
  aes(x = annee, y = lower),
  linetype = "dotted",
  colour = "#2D0084",
  linewidth = 1,
  alpha=1,
  inherit.aes = FALSE
) +
geom_line(
  data = newdata_RRP,
  aes(x = annee, y = upper),
  linetype = "dotted",
  colour = "#2D0084",
  linewidth = 1,
  alpha=1,
  inherit.aes = FALSE
)+
  scale_color_manual(
   name = "Droites de tendance",
   values = c(
    "Tendance générale RCS" = "#CD6839",
    "Tendance générale RRP" = "#2D0084"
  ))+
  labs(
    title = "Tendances temporelles générales des résultats de l'EQR Indice Biologique Macrophytes en Rivière (IBMR) par réseau.",
    subtitle = "Formule LMM = <i>resultat_eqr ~ annee_centree + (1 + annee_centree | code_station)</i>.<br>
    Les lignes en pointillés correspondent aux intervalles de confiance des tendances. ",
    caption="**EQR** = Ecological Quality Ratio, il s'agit d'un ratio entre le résultat observé et une valeur de référence.<br>
**<span style='color:#CD6839;'>RCS</span>**= Réseau de Contrôle de Surveillance.<br>
**<span style='color:#2D0084;'>RRP</span>**= Réseau de Référence Perenne.<br>
Une augmentation de la valeur de l'IBMR correspond à l'amélioration de l'état biologique.<br>
L'IBMR est étudié sur la periode 2013-2024 pour le réseau **<span style='color:#2D0084;'>RRP</span>** et 2007-2024 pour le réseau **<span style='color:#CD6839;'>RCS</span>**.",
    x = "Année",
    y = "IBMR"
  ) +
  theme_bw() +
  theme(plot.title=element_textbox_simple(size=16, 
                                            face="bold", 
                                            margin = margin(t=10,b = 10)),
        plot.subtitle = ggtext::element_textbox_simple(
          size = 13, margin = margin(b = 10)
        ),
        axis.title = element_text(size=14),
        axis.text = element_text(size=12),
        legend.title=element_text(size=13, face="bold"),
        legend.text=element_text(size=12),
        legend.position = "right",
        legend.title.position = "top",
        plot.caption = element_textbox_simple(
          size = 12,
          hjust = 0,      # 0 = aligné à gauche, 1 = à droite
          lineheight = 1.2,
          margin=margin(t=10, b=5),
          fill = "white", 
          box.color = "black",
          width = unit(0.80, "npc")
      )
        )


ggplot2::ggsave(
      filename = file.path("../Output/1_Tendance_indice/03_LMM/IBMR/Tendances_generales_IBMR.jpg"),
      plot     = Tendances_generales,
      width    = 13,
      height   = 10)
```


## RRP
```{r}
stations.coef_RRP <- coef(Modele_IBMR_RRP)$code_station
stations.coef_RRP$code_station <- rownames(stations.coef_RRP)

stations.coef_RRP <- coef(Modele_IBMR_RRP)$code_station %>%
  as.data.frame() %>%
  tibble::rownames_to_column("code_station") %>%
  dplyr::rename(
    intercept_station = `(Intercept)`,
    pente_station = annee_c
  )

lines_by_station_RRP <- BDD_IBMR_RRP %>%
  dplyr::select(code_station, libelle_station, code_departement, annee, annee_c) %>%
  dplyr::distinct() %>%
  dplyr::left_join(stations.coef_RRP, by = "code_station") %>%
  dplyr::mutate(
    fit = intercept_station + pente_station * annee_c
  )
```

Définir l'ordre d'apparition des stations en fonction de la valeur de la pente

```{r}
ordre_stations_RRP <- lines_by_station_RRP %>%
  distinct(libelle_station, pente_station) %>%
  arrange(pente_station) %>%
  pull(libelle_station)

BDD_IBMR_RRP <- BDD_IBMR_RRP %>%
  mutate(libelle_station = factor(libelle_station, levels = ordre_stations_RRP))

lines_by_station_RRP <- lines_by_station_RRP %>%
  mutate(libelle_station = factor(libelle_station, levels = ordre_stations_RRP))

lab_facet_RRP <- BDD_IBMR_RRP %>%
  ungroup() %>% 
  distinct(libelle_station, code_departement) %>%
  mutate(label = paste0(libelle_station, " (", code_departement, ")")) %>%
  select(-code_departement) %>%   # garder seulement 2 colonnes
  tibble::deframe()

```

###Graphique facetté
```{r}
classes_IBMR <- valeurs_seuils %>%
  filter(indice=="IBMR") %>% 
  dplyr::rename(
    classe_borne_inf = seuil_bas,
    classe_borne_sup = seuil_haut,
    classe_libelle   = classe,
    classe_couleur   = couleur
  )

library(colorspace)

classes_IBMR$classe_couleur <- lighten(classes_IBMR$classe_couleur, 0.7)
classes_IBMR$classe_libelle <- factor(
  classes_IBMR$classe_libelle,
  levels = c("Mauvais", "Médiocre", "Moyen", "Bon", "Très bon")
)

```

```{r}
n_facettes_RRP <- length(unique(BDD_IBMR_RRP$libelle_station))
n_col_RRP <-5
n_lignes_RRP <- ceiling(n_facettes_RRP / n_col_RRP)
hauteur_plot_RRP <- n_lignes_RRP * 3
largeur_plot_RRP <- n_col_RRP * 4

Tendance_IBMR_RRP <- ggplot(BDD_IBMR_RRP, aes(x = annee, y = resultat_eqr)) +
  geom_rect(
    data = classes_IBMR,
    aes(
      ymin = classe_borne_inf,
      ymax = classe_borne_sup,
      fill = classe_libelle
    ),
    xmin = -Inf,
    xmax = Inf,
    alpha = 0.6,
    inherit.aes = FALSE
  ) +
  scale_fill_manual(
    name = "État biologique",
    values = setNames(classes_IBMR$classe_couleur, classes_IBMR$classe_libelle),
    guide = guide_legend(reverse = TRUE)
  ) +
  geom_line(
  data = newdata_RRP,
  aes(x = annee, y = lower),
  linetype = "dotted",
  colour = "#2D0084",
  linewidth = 0.6,
  alpha=0.6,
  inherit.aes = FALSE
) +
geom_line(
  data = newdata_RRP,
  aes(x = annee, y = upper),
  linetype = "dotted",
  colour = "#2D0084",
  linewidth = 0.6,
  alpha=0.6,
  inherit.aes = FALSE
)+
  geom_line(
    data = newdata_RRP,
    aes(x = annee, y = fit, colour = "Tendance générale des stations RRP"),
    linewidth = 1,
    linetype = 2,
    inherit.aes = FALSE
  ) +
  geom_point(colour = "#990099FF", size = 2
             , alpha = 0.7) +
  geom_line(
    data = lines_by_station_RRP,
    aes(x = annee, y = fit, colour = "Tendance de la station"),
    linewidth = 1
  ) +
  scale_color_manual(
  name = "Droites de tendance",
  values = c(
    "Tendance générale des stations RRP" = "#2D0084",
    "Tendance de la station" = "#990099FF"
  ))+
  scale_x_continuous(
    breaks = seq(
      from = min(BDD_IBMR_RRP$annee),
      to   = max(BDD_IBMR_RRP$annee),
      by   = 2
    ))+
  facet_wrap(~libelle_station, ncol= n_col_RRP, labeller = labeller(libelle_station = lab_facet_RRP))+
  labs(
    title = "Tendance temporelle des résultats de l'EQR Indice Biologique Macrophytes en Rivières (IBMR) pour les stations <span style='color:#2D0084;'>RRP</span>, et comparaison avec la tendance générale des stations <span style='color:#2D0084;'>RRP</span>.",
    subtitle="Formule LMM = <i>resultat_eqr ~ annee_centree + (1 + annee_centree | code_station)</i>.<br>
    Les lignes en pointillés délimitent l'intervalle de confiance de la tendance générale du réseau **<span style='color:#2D0084;'>RRP</span>**.",
    caption="**EQR** = Ecological Quality Ratio, il s'agit d'un ratio entre le résultat observé et une valeur de référence.<br>
**<span style='color:#2D0084;'>RRP</span>**= Réseau de Référence Perenne.<br>
Une augmentation de la valeur de l'IBMR correspond à l'amélioration de l'état biologique.<br>
L'IBMR est étudié sur la periode 2013-2024.",
    x = "Année",
    y = "IBMR"
  ) +
  theme_bw() +
  theme(
    plot.title=element_textbox_simple(
      size = 20,           # Taille du texte
      face = "bold",       # Gras
      margin = margin(b = 10) ),# Marge en bas
    plot.subtitle=element_markdown(
      size = 16,           # Taille du texte
      lineheight = 1.3,
      margin = margin(b = 5) ),# Marge en bas
    axis.title = element_text(size=16),
    legend.title=element_text(size=16, face="bold"),
    legend.text=element_text(size=14),
    legend.position = "bottom",
    legend.title.position = "top",
    strip.text = element_textbox_simple(size=11, margin = margin(10,3,7,3),halign = 0.5),
    strip.background = element_rect(fill = NA, color = "black"),
    plot.caption = element_textbox_simple(
      size = 13,
      hjust = 0,      # 0 = aligné à gauche, 1 = à droite
      lineheight = 1.3,
      margin=margin(b=5),
      box.color = "black",
      linewidth = 0.7,
      width = unit(0.80, "npc")
  ))

ggplot2::ggsave(
      filename = file.path("../Output/1_Tendance_indice/03_LMM/IBMR/Tendance_IBMR_RRP.jpg"),
      plot     = Tendance_IBMR_RRP,
      width    = largeur_plot_RRP,
      height   = hauteur_plot_RRP)
```




## RCS
```{r}
stations.coef_RCS <- coef(Modele_IBMR_RCS)$code_station
stations.coef_RCS$code_station <- rownames(stations.coef_RCS)

stations.coef_RCS <- coef(Modele_IBMR_RCS)$code_station %>%
  as.data.frame() %>%
  tibble::rownames_to_column("code_station") %>%
  dplyr::rename(
    intercept_station = `(Intercept)`,
    pente_station = annee_c
  )

lines_by_station_RCS <- BDD_IBMR_RCS_clean %>%
  dplyr::select(code_station, libelle_station, code_departement, annee, annee_c) %>%
  dplyr::distinct() %>%
  dplyr::left_join(stations.coef_RCS, by = "code_station") %>%
  dplyr::mutate(
    fit = intercept_station + pente_station * annee_c
  )
```

Définir l'ordre d'apparition des stations en fonction de la valeur de la pente
```{r}
ordre_stations_RCS <- lines_by_station_RCS %>%
  distinct(libelle_station,pente_station) %>%
  arrange(pente_station) %>%
  pull(libelle_station)

BDD_IBMR_RCS_clean <- BDD_IBMR_RCS_clean %>%
  mutate(libelle_station = factor(libelle_station, levels = ordre_stations_RCS))

lines_by_station_RCS <- lines_by_station_RCS %>%
  mutate(libelle_station = factor(libelle_station, levels = ordre_stations_RCS))

lab_facet_RCS <- BDD_IBMR_RCS_clean %>%
  ungroup() %>% 
  distinct(libelle_station, code_departement) %>%
  mutate(label = paste0(libelle_station, " (", code_departement, ")")) %>%
  select(-code_departement) %>%   # garder seulement 2 colonnes
  tibble::deframe()
```

### Graphiques facettés
```{r}
n_facettes_RCS <- length(unique(BDD_IBMR_RCS_clean$libelle_station))
n_col_RCS <-6
n_lignes_RCS <- ceiling(n_facettes_RCS / n_col_RCS)
hauteur_plot_RCS <- n_lignes_RCS * 2.5
largeur_plot_RCS <- n_col_RCS * 3

Tendance_IBMR_RCS <- ggplot(BDD_IBMR_RCS_clean, aes(x = annee, y = resultat_eqr)) +
  geom_rect(
    data = classes_IBMR,
    aes(
      ymin = classe_borne_inf,
      ymax = classe_borne_sup,
      fill = classe_libelle
    ),
    xmin = -Inf,
    xmax = Inf,
    alpha = 0.5,
    inherit.aes = FALSE
  ) +
  scale_fill_manual(
    name = "État biologique",
    values = setNames(classes_IBMR$classe_couleur, classes_IBMR$classe_libelle),
    guide = guide_legend(reverse = TRUE)
  ) +
  geom_line(
  data = newdata_RCS,
  aes(x = annee, y = lower),
  linetype = "dotted",
  colour = "#CD6839",
  linewidth = 0.6,
  alpha=0.6,
  inherit.aes = FALSE
) +
geom_line(
  data = newdata_RCS,
  aes(x = annee, y = upper),
  linetype = "dotted",
  colour = "#CD6839",
  linewidth = 0.6,
  alpha=0.6,
  inherit.aes = FALSE
) +
  geom_line(
    data = newdata_RCS,
    aes(x = annee, y = fit, colour = "Tendance générale des station RCS"),
    linewidth = 1,
    linetype=2,
    inherit.aes = FALSE
  ) +
  geom_point(colour = "#990099FF", size = 2.5
             , alpha = 0.7) +
  geom_line(
    data = lines_by_station_RCS,
    aes(x = annee, y = fit, colour = "Tendance de la station"),
    linewidth = 1.3
  ) +
  scale_color_manual(
   name = "Droites de tendance",
   values = c(
    "Tendance générale des station RCS" = "#CD6839",
    "Tendance de la station" = "#990099FF"
  ))+
  facet_wrap(~libelle_station, ncol=n_col_RCS, labeller = labeller(libelle_station = lab_facet_RCS))+
  labs(
    title = "Tendance temporelle des résultats de l'EQR Indice Biologique Macrophytes en Rivières (IBMR) pour les stations <span style='color:#CD6839;'>RCS</span>, et comparaison avec la tendance générale des stations **<span style='color:#CD6839;'>RCS</span>**.",
    subtitle="Formule clmm= <i>libelle_classe ~ annee_centree + (1 + annee_centree | code_station)</i>.<br>
    Les lignes en pointillés délimitent l'intervalle de confiance de la tendance générale du réseau <span style='color:#CD6839;'>RCS</span>.",
    caption="**EQR** = Ecological Quality Ratio, il s'agit d'un ratio entre le résultat observé et une valeur de référence.<br>
**<span style='color:#CD6839;'>RCS</span>**= Réseau de Contrôle de surveillance.<br>
Une augmentation de la valeur de l'IBMR correspond à l'amélioration de l'état biologique.<br>
L'IBMR est étudié sur la periode 2007-2024.",
    x = "Année",
    y = "IBMR"
  ) +
  theme_bw() +
  theme(
    plot.title=element_textbox_simple(
      size = 20,           # Taille du texte
      face = "bold",       # Gras
      margin = margin(b = 10) ),# Marge en bas
    plot.subtitle=element_markdown(
      size = 16,           # Taille du texte
      lineheight = 1.3,
      margin = margin(b = 5) ),# Marge en bas
    axis.title = element_text(size=16),
    legend.title=element_text(size=16, face="bold"),
    legend.text=element_text(size=14),
    legend.position = "bottom",
    legend.title.position = "top",
    strip.text = element_textbox_simple(size=11, margin = margin(10,3,7,3),halign = 0.5),
    strip.background = element_rect(fill = NA, color = "black"),
    plot.caption = element_textbox_simple(
      size = 13,
      hjust = 0,      # 0 = aligné à gauche, 1 = à droite
      lineheight = 1.3,
      margin=margin(b=5),
      fill = "white", 
      box.color = "black",
      width = unit(0.80, "npc")
  ))

ggplot2::ggsave(
      filename = file.path("../Output/1_Tendance_indice/03_LMM/IBMR/Tendance_IBMR_RCS.jpg"),
      plot     = Tendance_IBMR_RCS,
      width    = largeur_plot_RCS,
      height   = hauteur_plot_RCS)
```


# Sauvegarder tous les résultats

## résultats globaux région
```{r}
donnees_models_IBMR <- bind_rows(donnees_model_IBMR_RRP, donnees_model_IBMR_RCS)
```

## pentes des stations
```{r}
export_pente_df_RRP <- coef(Modele_IBMR_RRP)$code_station %>%
  as.data.frame() %>%
  tibble::rownames_to_column("code_station") %>%
  rename(
    intercept = `(Intercept)`,
    pente = annee_c
  ) %>% 
  mutate(reseaux="RRP")%>% 
  select(-intercept)

export_pente_df_RCS <- coef(Modele_IBMR_RCS)$code_station %>%
  as.data.frame() %>%
  tibble::rownames_to_column("code_station") %>%
  rename(
    intercept = `(Intercept)`,
    pente = annee_c
  ) %>% 
  mutate(reseaux="RCS")%>% 
  select(-intercept)
```

```{r}
pentes_IBMR <- bind_rows(export_pente_df_RRP, export_pente_df_RCS)

pentes_LMM_IBMR<- BDD_IBMR %>% 
  dplyr::select(code_station, libelle_station, code_departement) %>%
  dplyr::distinct() %>%
  dplyr::left_join(pentes_IBMR, by = "code_station") %>%
  mutate(pente=round(pente,4)) %>% 
  mutate(libelle_indice="IBMR")
```

```{r}
lines_by_station_IBMR <- bind_rows(lines_by_station_RRP, lines_by_station_RCS)

lines_by_station_IBMR <- lines_by_station_IBMR %>% 
  mutate(libelle_indice="IBMR")
```

```{r}
data_graph_RRP_IBMR <- newdata_RRP %>% 
  mutate(libelle_indice="IBMR")

data_graph_RCS_IBMR <- newdata_RCS %>% 
  mutate(libelle_indice="IBMR")
```

```{r}
saveRDS(donnees_models_IBMR, here("Data/clean_data/Models","donnees_models_IBMR.rds"))
saveRDS(pentes_LMM_IBMR, here("Data/clean_data/Models","pentes_LMM_IBMR.rds"))
saveRDS(lines_by_station_IBMR, here("Data/clean_data/Models","lines_by_station_IBMR.rds"))
saveRDS(data_graph_RRP_IBMR, here("Data/clean_data/Models","data_graph_RRP_IBMR.rds"))
saveRDS(data_graph_RCS_IBMR, here("Data/clean_data/Models","data_graph_RCS_IBMR.rds"))
```